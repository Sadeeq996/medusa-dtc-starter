/**
 * Theme Constants
 * Centralized theme configuration to avoid duplication
 */

/**
 * LocalStorage key for theme preset preference
 */
export const THEME_PRESET_STORAGE_KEY = 'theme-preset';

/**
 * Default theme preset to use when no preference is saved
 */
export const DEFAULT_THEME_PRESET = 'vibrant' as const;

